<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>おみくじ</title>
    <style>
    .example4 {
      background: linear-gradient(
        to right,
        #e60000,
        #f39800,
        #fff100,
        #009944,
        #0068b7,
        #1d2088,
        #920783,
        #e60000
      );
      background-size: 400% 100%;
      animation: rainbow 5s linear infinite;
      height: 500px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2em;
      color: white;
      font-weight: bold;
    }

    @keyframes rainbow {
      0% { background-position: 0% 50%; }
      100% { background-position: 100% 50%; }
    }
  </style>
</head>
<body>
    <div class="example4">
    <p><br>今日の運勢は<%= result %>です．</p></p>
    <p>作成者からのコメント：<%= comment %></p>
</body>
</html>