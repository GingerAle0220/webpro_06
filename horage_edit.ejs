<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>Edit Room <%= data.number %></title>
    <link rel="stylesheet" href="/css/horage.css">
</head>
<body>

    <div class="form-wrapper">
        <h2 style="color:#c00; text-align:center; margin-bottom:30px;">記録を書き換える: Room <%= data.number %></h2>
        
        <form action="/horage/<%= data.number %>/update" method="POST">
            
            <!-- Numberは変更不可として表示のみにするか、hiddenで送るのが一般的 -->
            <div class="form-group">
                <label>Game Name</label>
                <input type="text" name="game_name" value="<%= data.game_name %>" required>
            </div>

            <div class="form-group">
                <label>Developer</label>
                <input type="text" name="developer" value="<%= data.developer %>">
            </div>

            <div class="form-group">
                <label>Image URL</label>
                <input type="text" name="image" value="<%= data.image %>">
            </div>

            <div class="form-group">
                <label>Overview</label>
                <textarea name="overview" rows="5"><%= data.overview %></textarea>
            </div>

            <div class="form-group">
                <label>My Comment</label>
                <textarea name="my_comment2" rows="4"><%= data.my_comment2 %></textarea>
            </div>

            <div style="text-align:right;">
                <a href="/horage/<%= data.number %>" style="color:#888; margin-right:20px;">キャンセル</a>
                <button type="submit" class="btn" style="background:#005;">更新</button>
            </div>

        </form>
    </div>

</body>
</html>